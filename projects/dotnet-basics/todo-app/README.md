# 综合练习 —— 命令行待办工具
- 实现以下命令（用 `dotnet run -- <command>` 传参）：
  - `add "任务内容"` → 添加任务（内存存储，演示 DI）
  - `list` → 显示所有任务
  - `done <id>` → 标记完成（输出日志）
- 要求：使用自己写的 DI 容器、配置（如从 JSON 读取最大任务数）、日志
- 将任务持久化到 JSON 文件